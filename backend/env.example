PORT=3000

# Optional: provide a real-time gold price API key
# When omitted, the app uses a fallback price
GOLD_API_KEY=

# Cache TTL for gold price (ms)
GOLD_CACHE_TTL_MS=600000

# Fallback price (USD per gram) when no API key or request fails
FALLBACK_GOLD_PRICE_USD_PER_GRAM=60


